# Your Project 1 map

Create `student_map.json` here (or change the `map` target to use your own
location). It must be an OccupancyGrid-like JSON object meeting the map rules
in `docs/PROJECT1_ASTAR.md`. This starter deliberately does not provide a
completed map: creating one is part of Project 1.
